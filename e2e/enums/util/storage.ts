// Storage-state file paths (auth sessions). Written by e2e/tests/auth.setup.ts,
// consumed by the "e2e" Playwright project. .auth/ is gitignored.
export enum StorageStatePaths {
  ADMIN = ".auth/admin.json",
}
